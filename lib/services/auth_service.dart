class AuthService {
  Future<bool> login(String email, String password) async {
    // পরে Supabase Login এখানে যুক্ত হবে
    return true;
  }

  Future<bool> register(
    String name,
    String email,
    String password,
  ) async {
    // পরে Supabase Register এখানে যুক্ত হবে
    return true;
  }

  Future<void> logout() async {
    // পরে Logout যুক্ত হবে
  }
}
